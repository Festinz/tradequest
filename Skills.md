# Skills.md — 투자 데이터 분석 규칙

> TradeQuest Insights 의 분석 엔진이 따르는 규칙 문서
> 모든 시각화·인사이트·리포트는 이 문서의 규칙을 일관되게 적용해 생성된다.

---

## 0. 문서의 역할

본 Skills.md 는 **데이터 → 분석 → 시각화 → 인사이트** 의 전 흐름에 대한 **선언적 규칙(declarative rules)** 을 정의한다.
투자 데이터의 형태가 매번 달라도, 동일한 규칙 세트로 일관된 대시보드를 자동 생성하기 위한 단일 진실 공급원(SSOT) 이다.

본 문서를 수정하면 코드 변경 없이도 분석 기준·차트 종류·리포트 흐름이 바뀐다.

---

## 1. 입력 데이터 인터페이스

분석 엔진은 다음 4가지 표준 형태의 입력만 받는다. 외부 데이터는 어댑터를 통해 이 형태로 정규화된다.

### 1.1 OHLCV 시계열
```ts
type Candle = { time: 'YYYY-MM-DD'; open: number; high: number; low: number; close: number; volume: number };
type OHLCV = Candle[];
```

### 1.2 종목 메타
```ts
type Stock = { ticker: string; name: string; market: 'KOSPI'|'KOSDAQ'|'NASDAQ'|'NYSE'; sector?: string; per?: number; pbr?: number; roe?: number; eps?: number; dividend_yield?: number };
```

### 1.3 포지션
```ts
type Position = { ticker: string; qty: number; avg_price: number; current_price: number };
```

### 1.4 거래 기록
```ts
type Trade = { ticker: string; side: 'buy'|'sell'; qty: number; price: number; opened_at: string; closed_at?: string; pnl?: number };
```

---

## 2. 지표 계산 규칙

규칙 ID 는 `IND-XXX` 형식. 모든 계산은 결정적(deterministic) 이며 동일 입력 → 동일 출력.

### IND-001 단순이동평균 (SMA)
- **수식**: `SMA(n)[t] = (close[t] + close[t-1] + ... + close[t-n+1]) / n`
- **기본 윈도우**: 5, 20, 60, 120 일
- **NULL 처리**: 데이터가 n일 미만일 때 NULL 반환 (0 으로 채우지 않음)

### IND-002 지수이동평균 (EMA)
- **수식**: `EMA(n)[t] = α × close[t] + (1-α) × EMA(n)[t-1]`, `α = 2/(n+1)`
- **시드**: 첫 EMA 값 = SMA(n)[t=n-1]
- **기본 윈도우**: 12, 26 일

### IND-003 RSI (14일)
- **수식**: `RSI = 100 - 100 / (1 + RS)`, `RS = 평균상승폭 / 평균하락폭` (윈도우 14)
- **해석**:
  - `RSI > 70` → 과매수 (insight `INS-001` 발동)
  - `RSI < 30` → 과매도 (insight `INS-002` 발동)
  - `30 ≤ RSI ≤ 70` → 중립

### IND-004 MACD
- **수식**: `MACD = EMA(12) - EMA(26)`, `Signal = EMA(MACD, 9)`, `Histogram = MACD - Signal`
- **신호**:
  - MACD 가 Signal 을 위로 돌파 → 골든크로스
  - MACD 가 Signal 을 아래로 돌파 → 데드크로스

### IND-005 볼린저 밴드 (20일, 2σ)
- **수식**: `중심선 = SMA(20)`, `상단 = 중심선 + 2σ`, `하단 = 중심선 - 2σ` (σ = 20일 표준편차)
- **활용**: 가격이 상단을 벗어나면 추세 강세 신호, 하단 이탈은 과매도 가능성

### IND-006 거래량 OBV (On-Balance Volume)
- **수식**: `OBV[t] = OBV[t-1] + volume[t]` (close 상승 시) / `- volume[t]` (하락 시) / 동일 시 0
- **활용**: 가격 추세와 OBV 다이버전스 시 추세 약화 신호

### IND-007 변동성 (Historical Volatility, 연환산)
- **수식**: `HV = stdev(log returns) × √252`
- **윈도우**: 20, 60일
- **활용**: 포트폴리오 리스크 측정

### IND-008 누적 수익률
- **수식**: `Return[t] = (close[t] / close[0]) - 1`

### IND-009 최대낙폭 (MDD)
- **수식**: `MDD = max((peak - trough) / peak)` (윈도우 내 모든 peak-trough 쌍)

### IND-010 샤프 비율 (단순화)
- **수식**: `Sharpe = (평균일간수익률 - 무위험수익률) / 일간수익률표준편차 × √252`
- **무위험수익률**: 한국 3년물 국채 수익률 (기본 3.5%)

### IND-011 가치 지표 (Fundamental)
- **PER**: `시가총액 / 당기순이익`
- **PBR**: `시가총액 / 자본총계`
- **ROE**: `당기순이익 / 자본총계 × 100`
- **배당수익률**: `1주당 배당금 / 주가 × 100`

### IND-012 베타 (시장대비 변동성)
- **수식**: `β = cov(자산수익률, 시장수익률) / var(시장수익률)`
- **시장**: KOSPI 200 (KOSPI 종목) / KOSDAQ 150 (KOSDAQ 종목)
- **윈도우**: 1년

---

## 3. 시각화 선택 기준

차트 ID 는 `VIZ-XXX` 형식. 데이터 타입과 분석 의도에 따라 자동 매핑.

| 데이터 형태 | 분석 의도 | 차트 종류 (VIZ ID) |
|---|---|---|
| OHLCV 시계열 | 가격 추세 | 캔들스틱 (`VIZ-001`) |
| 단일 시계열 (1지표) | 추이 | 라인 차트 (`VIZ-002`) |
| 다중 시계열 (지표 비교) | 동조성 | 멀티라인 (`VIZ-003`) |
| 거래량 (시계열) | 변화 | 히스토그램 (`VIZ-004`) |
| 종목 간 수익률 비교 | 상대성과 | 정규화 멀티라인 100=시작 (`VIZ-005`) |
| 포트폴리오 구성 비중 | 점유율 | 도넛 (`VIZ-006`) |
| 섹터별 성과 | 군집 비교 | 트리맵 (`VIZ-007`) |
| 종목 간 상관관계 | 분산 효과 | 히트맵 (`VIZ-008`) |
| 단일 지표 vs 임계 | 현재 위치 | 게이지 (`VIZ-009`) |
| 매매 시점 | 진입/청산 | 캔들 + 마커 (`VIZ-010`) |
| 누적 수익률 + 드로다운 | 백테스트 결과 | 면적 + 음영 (`VIZ-011`) |

### 색상 규칙 (한국 시장 컨벤션)
- **상승**: `#EF4444` (빨강)
- **하락**: `#3B82F6` (파랑)
- **중립**: `#94A3B8` (회색)
- **EXP/하이라이트**: `#FBBF24` (노랑)

### 그리드/타이포그래피
- **폰트**: Pretendard Variable (한국어), JetBrains Mono (숫자)
- **격자**: `rgba(226, 232, 240, 0.3)` (저강도)
- **숫자 포맷**: `1,084,210` (천 단위 콤마, 소수 0~2자리)

---

## 4. 인사이트 생성 규칙

규칙 ID 는 `INS-XXX`. 각 규칙은 **조건 → 메시지 + 강도(severity) + 액션** 구조.

### INS-001 과매수 경고
- **조건**: `IND-003 RSI > 70` 이고 직전 5일 RSI 평균이 65 이상
- **메시지**: "{종목명} 의 RSI 가 {RSI값} 으로 과매수 영역. 단기 조정 가능성 점검 필요."
- **강도**: `warning`
- **추천 액션**: "익절 분할 또는 신규 매수 보류"

### INS-002 과매도 기회
- **조건**: `IND-003 RSI < 30` 이고 가격이 IND-005 볼린저 하단 이탈
- **메시지**: "{종목명} 이 과매도 + 볼린저 하단 이탈. 반등 가능성 모니터링."
- **강도**: `info`
- **추천 액션**: "분할 매수 검토. 손절선 설정 필수"

### INS-003 골든크로스
- **조건**: 직전 5거래일 내 IND-004 MACD 가 Signal 을 상향 돌파
- **메시지**: "{종목명} MACD 골든크로스 발생. 단기 상승 모멘텀 형성 가능."
- **강도**: `bullish`
- **추천 액션**: "매수 시 진입 근거에 모멘텀 명시"

### INS-004 데드크로스
- **조건**: 직전 5거래일 내 IND-004 MACD 가 Signal 을 하향 돌파
- **메시지**: "{종목명} MACD 데드크로스. 단기 약세 가능성."
- **강도**: `bearish`
- **추천 액션**: "보유 시 손절선 점검"

### INS-005 거래량 폭증 + 가격 상승
- **조건**: 거래량이 20일 평균의 200% 초과 + close[t] > close[t-1]
- **메시지**: "{종목명} 평균 대비 {배수}배 거래량 + 상승. 강세 시그널."
- **강도**: `bullish`

### INS-006 거래량 폭증 + 가격 하락
- **조건**: 거래량이 20일 평균의 200% 초과 + close[t] < close[t-1]
- **메시지**: "{종목명} 평균 대비 {배수}배 거래량 + 하락. 패닉셀 가능성."
- **강도**: `warning`

### INS-007 OBV 다이버전스
- **조건**: 직전 20일 가격은 신고가 갱신했는데 OBV 는 갱신 못함
- **메시지**: "가격 신고가 vs OBV 약세 다이버전스. 추세 약화 신호."
- **강도**: `warning`

### INS-008 포트폴리오 집중 위험
- **조건**: 단일 종목이 포트폴리오 평가금액의 30% 초과
- **메시지**: "{종목명} 비중이 {비중}%. 단일 종목 리스크 노출."
- **강도**: `warning`
- **추천 액션**: "분산투자 검토"

### INS-009 섹터 편중
- **조건**: 단일 섹터가 평가금액의 50% 초과
- **메시지**: "{섹터명} 섹터 편중 {비중}%. 섹터 사이클 리스크."
- **강도**: `info`

### INS-010 변동성 급등
- **조건**: 20일 HV (`IND-007`) 가 60일 HV 의 1.5배 초과
- **메시지**: "최근 변동성이 평소의 {배수}배. 포지션 사이즈 축소 검토."
- **강도**: `warning`

### INS-011 손실 누적 (감정 보호)
- **조건**: 직전 5거래일 PnL 합 < -5%
- **메시지**: "최근 5일 누적 -{손실}%. 감정 매매 주의. 한 번 쉬어 가는 것도 전략."
- **강도**: `info`

### INS-012 추세 정렬 (Aligned Trend)
- **조건**: SMA(5) > SMA(20) > SMA(60) (정배열)
- **메시지**: "{종목명} 이동평균선 정배열. 중장기 상승 추세 유지."
- **강도**: `bullish`

---

## 5. 리포트 구성 흐름

리포트는 4단계 파이프라인을 거친다. 각 단계는 stateless 하며 입력→출력만 정의된다.

```
[Raw Data] → [정규화] → [지표 계산] → [인사이트 추출] → [리포트 조립]
              §1          §2            §4               §5
```

### 5.1 정규화 단계
- 외부 데이터 (KIS / Yahoo / Naver) → §1 표준 인터페이스로 변환
- 누락 거래일 채움(forward fill 금지 — NULL 유지)
- 시계열 정렬 (오래된 → 최신)

### 5.2 지표 계산 단계
- §2 의 모든 IND 규칙을 적용해 derived dataset 생성
- 동일 데이터에 동일 규칙 → 동일 결과 (캐시 가능, 60초 SWR)

### 5.3 인사이트 추출 단계
- §4 의 모든 INS 규칙 평가 → `triggered_insights[]` 생성
- severity 순 정렬: `warning > bearish > bullish > info`
- 중복 종목·규칙 제거 (최근 1건만 유지)

### 5.4 리포트 조립 단계
대시보드 화면은 정해진 컴포넌트 슬롯에 데이터 매핑:

| 슬롯 | 입력 | 차트 | 우선순위 |
|---|---|---|---|
| Hero | 포트폴리오 총자산 + 일간 손익 | KPI 카드 | 1 |
| Top Insights | severity 상위 3건 | 리스트 | 2 |
| Price Chart | 선택 종목 OHLCV | VIZ-001 캔들 + VIZ-010 마커 | 3 |
| Indicators | RSI, MACD | VIZ-002 라인 + VIZ-009 게이지 | 4 |
| Holdings | positions[] | VIZ-006 도넛 | 5 |
| Sector | 보유 종목 섹터별 합계 | VIZ-007 트리맵 | 6 |
| Correlation | 보유 종목간 일간수익률 | VIZ-008 히트맵 | 7 |
| Backtest | 가상 매매 누적 수익 | VIZ-011 면적 | 8 |

---

## 6. AI 인사이트 생성 (LLM 보강)

§4 의 결정적(deterministic) 규칙 위에, LLM 으로 자연어 해설을 보강한다.

### 6.1 사용 모델
- **일반 해설**: Claude Haiku 4.5 (저비용, 빠름)
- **주간 회고**: Claude Sonnet 4.6 (월 4회 한정)

### 6.2 프롬프트 가드레일
- 항상 한국어 반말, 짧고 명확
- 절대 특정 종목의 매수/매도를 단정적으로 권하지 않음 (법적 리스크)
- 손절을 부정적인 일로 만들지 않고 학습 신호로 다룸
- 한국 시장 컨벤션 (빨강↑/파랑↓) 따름

### 6.3 호출 빈도 제한
- 사용자별 일일 30회 (`ai_messages` 테이블에서 카운트)
- 한도 도달 시 결정적 인사이트만 표시 + 친절한 안내

### 6.4 비용 가드
- 응답 토큰 상한 600
- 컨텍스트 트리밍 (가장 최근 20일치 데이터만 첨부)
- 월 사용자당 목표: $0.10 이하

---

## 7. 확장성 — 새 규칙 추가 절차

본 Skills.md 에 규칙을 추가하면 코드 변경 없이 새로운 분석/시각화/인사이트가 활성화된다.

### 새 지표 (IND) 추가
1. §2 에 `IND-XXX` 항목 신설 (수식, 윈도우, NULL 처리 명시)
2. `lib/skills/registry.ts` 의 `INDICATOR_FNS` 에 동일 ID 함수 등록
3. 자동으로 차트 옵션에 노출

### 새 차트 (VIZ) 추가
1. §3 의 매핑 표에 새 행 추가
2. `components/chart/` 에 새 컴포넌트 + `VIZ_REGISTRY` 등록
3. 리포트 슬롯이 자동으로 새 차트 선택 가능

### 새 인사이트 (INS) 추가
1. §4 에 `INS-XXX` 정의 (조건/메시지/강도/액션)
2. `lib/skills/insights.ts` 의 `EVALUATORS` 에 등록
3. 다음 분석 사이클부터 자동 발동

---

## 8. 데이터 출처와 라이선스

| 데이터 | 출처 | 라이선스 | 사용 범위 |
|---|---|---|---|
| 한국 일봉 | 네이버 금융 / Yahoo Finance | 비공식·공개 데이터 | 학습/개인 |
| 실시간 시세 | KIS Developers API | 본인 계좌 한정 | 개인 모드만 |
| 종목 마스터 | KRX 공개 정보 | 공공데이터 | 자유 |
| 차트 라이브러리 | TradingView Lightweight Charts | Apache 2.0 | 자유 |
| AI | Anthropic Claude API | 상업적 사용 가능 | 약관 준수 |

---

## 부록 A. 인사이트 트리거 우선순위

동시에 여러 인사이트가 발동될 때 표시 순서:
1. `INS-008` 포트폴리오 집중 위험 (warning)
2. `INS-001` 과매수 경고 (warning)
3. `INS-007` OBV 다이버전스 (warning)
4. `INS-010` 변동성 급등 (warning)
5. `INS-006` 패닉셀 가능성 (warning)
6. `INS-004` 데드크로스 (bearish)
7. `INS-003` 골든크로스 (bullish)
8. `INS-005` 거래량 + 상승 (bullish)
9. `INS-012` 정배열 (bullish)
10. `INS-002` 과매도 기회 (info)
11. `INS-009` 섹터 편중 (info)
12. `INS-011` 손실 누적 (info)

---

## 부록 B. 버전

- **v0.1** (2026-04-26) — 초기 버전. IND 12개 + VIZ 11개 + INS 12개.
- 향후: 채권/ETF/암호화폐 분기, 다국가 시장 추가, 백테스팅 규칙 (`BT-XXX`) 신설.
